import React from 'react';

const Title = ({ text }) => {
  return (
    <h1 className="text-4xl font-bold  font-[Poppins] text-center text-orange-500 mt-6 ">
      {text}
    </h1>
  );
};

export default Title;
